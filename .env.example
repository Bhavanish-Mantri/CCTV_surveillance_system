# .env — Environment overrides (optional)
# Copy this file to .env and fill in your values.
# python-dotenv will load it automatically when you add load_dotenv() to app.py.

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=cctv_attendance

FLASK_DEBUG=true
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

SECRET_KEY=change-me-to-something-random

FACE_THRESHOLD=0.5
FRAME_SCALE=0.25
PROCESS_N_FRAMES=3
ATTENDANCE_COOLDOWN=10
